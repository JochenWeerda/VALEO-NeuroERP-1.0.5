import type { Node as FlowNode, Edge as FlowEdge, Connection as FlowConnection } from 'reactflow';

export type FlowNode = import('reactflow').Node;
export type FlowEdge = import('reactflow').Edge;
export type FlowConnection = import('reactflow').Connection;

export type { FlowNode, FlowEdge, FlowConnection }; 